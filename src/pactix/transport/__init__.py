"""Optional transport adapters.

Import the concrete transports from their subpackages so the core library never
depends on Kafka or HTTP libraries:

- ``pactix.transport.kafka`` (extra: ``kafka``)
- ``pactix.transport.http`` (extra: ``http``)
"""
